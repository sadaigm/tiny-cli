// Empty stub for Node stream subpaths (stream/web, stream/promises) that
// ink-web's plugin alias doesn't cover. The TUI never uses these at runtime;
// they only appear in transitive deps (fetch-blob) during dep optimization.
export default {};
